@model IEnumerable<string>
@{
    ViewData["Title"] = "Users";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Users</h2>

<table class="table-bordered table-striped table" id="UsersList" style="font-size: 14px;">
    <thead>
        <tr>
            <th>User Name</th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item</td>
            </tr>
        }
        </tbody>
    </table>